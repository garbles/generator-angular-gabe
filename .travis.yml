language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angular-gabe' ]; then cd .. && eval "mv $currentfolder generator-angular-gabe" && cd generator-angular-gabe; fi

