describe('<%= packageName %>', function () {
  beforeEach(module('<%= namespacedPackageName %>'));

  var $scope;

  beforeEach(inject(function ($rootScope) {
    $scope = $rootScope.$new();
  }));

  it('', function () {

  });
});
