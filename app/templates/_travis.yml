language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != '<%= packageName %>' ]; then cd .. && eval "mv $currentfolder <%= packageName %>" && cd <%= packageName %>; fi
